import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

//will render certain component
//"App" will be the main component .. will render to the DOM
//inside of the 'root' jsx
ReactDOM.render(<App />, document.getElementById('root'));